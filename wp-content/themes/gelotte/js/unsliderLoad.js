/* Loads the flexslider */
jQuery(document).ready(function($) {
    $('.the-slider').unslider( {
        autoplay: true,
        delay: 5000,        /* The duration between animations */
        speed: 750,         /* The duration of the animation */
    });
});